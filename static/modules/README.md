Frontend module CSS can be split here later without changing existing page behavior.

Keep moved rules byte-for-byte equivalent first; do not change copy, layout, or visual hierarchy during a path-only split.
